# pytest.ini - Configuration file for pytest

[pytest]
# Add the specified directory to the Python path.
# This allows pytest to locate and import modules from the "backend" directory.
# Example: If your tests need to import modules from the "backend" folder,
# they can do so without manually modifying the system path in your test files.
pythonpath = backend